<h2 class="page-header">
  Round
  <%= Round.current %>
  …
  fight!
</h2>
<table class="table leaderboard table-bordered" style="width:auto">
  <tbody>
    <% @scoreboard.sorted_scores.each do |team, score| %>
      <tr>
        <th><%= team %></th>
        <td><%= score.round %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<script type="text/javascript" charset="utf-8">
  var refresh = 20

  setInterval(function(){
    el = document.getElementById('js-refresh-countdown');
    currentTime = el.innerText;
    newTime = currentTime - 1;
    el.innerText = newTime;
    if (newTime == 0) location.reload();
  }, 1000)
  document.write("<p>Updating in <span id='js-refresh-countdown'>"+refresh+"</span> seconds</p>")
</script>
